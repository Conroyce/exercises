var matrixMultiplication = function(a,b) {
  if (a[0].length !== b.length) {
    return false;
  } else {
    var ansArr = [a.length,b[0].length];
    var ans = [];

    for (var i = 0; i < ansArr[0].length; i++) {
      var outerArr = [];
      for (var j = 0; j < ansArr[1].length; j++) {
        var innerArr = [];
        a[i]*b[j]
        var outerNum = 0;
        var innerNum = 0;
        while(outNum < ansArr)
      } 
    }
  }
};